from distutils.core import setup


setup(name='Homeless Shelter Backend',
      version='0.1',
      description='',
      author='',
      packages=[],
      entry_points={
          'console_scripts': ['shelter-backend=server.main:main'],
      })
